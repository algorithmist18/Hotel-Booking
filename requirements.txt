streamlit
pandas
numpy
scikit-learn
joblib
matplotlib
seaborn

# AI Advisor tab. The built-in engine calls the LLM provider's REST API
# directly using only the Python standard library — no extra install needed —
# so the AI Advisor works on Streamlit Cloud with just the packages above.
python-dotenv

# OPTIONAL: the full CrewAI framework. Only needed if you tick "Use the CrewAI
# framework" in the AI Advisor tab. It's heavy (chromadb, litellm) and its
# versions must be pinned, or Streamlit Cloud picks a chromadb/pydantic combo
# that fails to import ('unable to infer type for attribute
# "chroma_server_nofile"'). Uncomment to enable; the built-in engine does not
# need any of this.
# crewai[google-genai]==1.15.15
# chromadb==1.1.1
# pydantic==2.12.5
# pydantic-settings==2.15.0
